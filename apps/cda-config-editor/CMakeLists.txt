# $Id$
#
# Configuration for building the cda-config-editor application.
#

# Use the helper macro to set up the build:
add_cda_gui_app( cda-config-editor
   ICON ${CMAKE_SOURCE_DIR}/images/cda-config-editor.icns )
