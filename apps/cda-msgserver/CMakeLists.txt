#
# Configuration for building the cda-msgserver application.
#

# Use the helper macro to set up the build:
cda_add_gui_executable( cda-msgserver
   ICON ${CMAKE_SOURCE_DIR}/images/cda-msgserver.icns )
