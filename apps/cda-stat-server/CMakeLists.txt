#
# Configuration for building the cda-stat-server application.
#

# Use the helper macro to set up the build:
cda_add_gui_executable( cda-stat-server
   ICON ${CMAKE_SOURCE_DIR}/images/cda-stat-server.icns )
